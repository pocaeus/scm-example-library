# `scm-example-library`

Example Scheme library project (R7RS) with an opinionated directory structure.

```
.
├── LICENSE
├── README.md
├── bin
│   └── bin.scm
├── lib
│   ├── example.scm
│   └── example.sld
└── test
    └── test-example.scm
```
